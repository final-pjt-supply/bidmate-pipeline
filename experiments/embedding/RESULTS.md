# 임베딩 실험 결과 (BGE-M3 / Cloudflare Workers AI / OpenSearch knn)

## 1. 배경 / 목표

GPU 없이 스케일투제로가 가능한 방식으로 BGE-M3 임베딩을 실시간 파이프라인에
편입하기 위한 사전 검증. 로컬에서 전체 파이프라인(청킹 → 임베딩 → 인덱싱 →
검색)을 한 번 완주해서 각 단계가 실제로 맞물리는지, 검색 결과가 의미 있게
나오는지 확인한다.

## 2. 구성

| 단계 | 방식 |
|---|---|
| 데이터 | bidmate `extracted/daily/` 실데이터 68개 문서 |
| 청킹 | `embedding/chunker.py` (기존 로직 그대로 재사용, 변경 없음) — 2,624개 청크, truncated 319건 |
| 임베딩 | Cloudflare Workers AI `@cf/baai/bge-m3`, OpenAI 호환 `/v1/embeddings` API (`embedding/cloudflare_embedder.py`) |
| 벡터 저장소 | 로컬 OpenSearch(docker-compose, `db/docker-compose.yml`), `knn_vector` 필드(1024차원, HNSW, cosine similarity, Lucene 엔진) |

## 3. 임베딩 실행 결과

- 2,624개 청크 전체 임베딩: **121.8초, 청크당 46ms**
- 벡터 차원 1024, 전 청크 일관됨
- 비용: Cloudflare 공식 단가 $0.012/1M 입력 토큰 기준 코퍼스 전체(약 80만 토큰) **1센트 미만**

## 4. OpenSearch 적재

- 2,624건 전체 성공, 실패 0건
- 인덱스명: `embedding-experiment-chunks`

## 5. 검색 테스트 — knn (벡터 유사도)

3종 쿼리로 top-5 스코어/내용 확인 (`experiments/embedding/query_test.py`).

### 5-1. 코퍼스에 확실히 있는 내용
쿼리: `"eLoran 수신기 보정지도 지원 시스템 구축 공고의 입찰 참가자격은 무엇인가?"`

| 순위 | score | bid_id | 내용 |
|---|---|---|---|
| 1 | 0.7828 | R26BK01620506_001 | 공고서 표(수요기관/계약방법/낙찰자...) |
| 2 | 0.7783 | R26BK01624656_000 | 공고서 표(수요기관/품명...) |
| 3 | 0.7758 | R26BK01623284_000 | "3. 입찰참가자격" 섹션 헤더 |
| 4 | 0.7732 | R26BK01623172_000 | 입찰 설명서 안내문 |
| 5 | 0.7732 | R26BK01623760_000 | "입찰참가자격 등록" 관련 안내 |

정답 문서(R26BK01624671_000) 자체는 top-5에 없었으나, "입찰참가자격" 관련
내용을 가진 다른 문서들이 상위권에 잡혀 의미적 관련성은 확인됨. 정답 문서가
안 뜬 이유는 6절 참고(임베딩/검색 결함이 아니라 청킹 단계의 데이터 손실).

### 5-2. 일반 탐색형
쿼리: `"소프트웨어 개발 용역 공고"`

| 순위 | score | bid_id | 내용 |
|---|---|---|---|
| 1 | 0.7765 | R26BK01620445_000 | "기술용역 입찰설명서" |
| 2 | 0.7738 | R26BK01620154_000 | "기술용역 입찰공고" |
| 3 | 0.7721 | R26BK01620050_000 | "기술용역 입찰설명서" |
| 4 | 0.7707 | R26BK01622081_000 | "1. 입찰개요" |
| 5 | 0.7704 | R26BK01620154_000 | "용역명: 아산 풍기역지구..." |

"기술용역" 계열 문서가 상위권을 채워 탐색형 쿼리로 잘 작동.

### 5-3. 코퍼스에 없을 법한 엉뚱한 질문
쿼리: `"김치찌개 만드는 법"`

| 순위 | score | bid_id | 내용 |
|---|---|---|---|
| 1 | 0.6821 | R26BK01624022_000 | 창호 절단 규격 표 |
| 2 | 0.6771 | R26BK01623782_000 | 이미지 참조 나열 |
| 3 | 0.6735 | R26BK01624022_000 | 창호 배수구 가공 |
| 4 | 0.6728 | R26BK01624063_000 | 이미지 참조 나열 |
| 5 | 0.6716 | R26BK01624022_000 | 창호 유리고정테/잠금장치 |

**스코어가 1/2번(0.77~0.78대) 대비 3번(0.66~0.68대)에서 약 0.10 하락** —
knn 검색이 무관한 질문을 유의미하게 낮은 유사도로 구분함을 확인. 다만
완전 무관한 질문도 절대 스코어가 0.68까지 나오는 건 cosine similarity의
baseline이 BGE-M3 임베딩 공간에서 원래 높게 깔리기 때문 — 절대값보다
상대적 격차가 판별 신호로 유효함(스코어 임계값 하나로 자동 필터링하려면
추가 튜닝 필요).

## 6. 검색 테스트 — BM25 (텍스트 매치) 비교

5-1 쿼리를 knn 대신 `text` 필드 BM25(`match`) 쿼리로 재실행. 목적: 정답
문서(R26BK01624671_000)가 BM25로는 상위에 뜨는지 확인해 하이브리드 검색
도입 근거를 만드는 것.

**결과: BM25로도 R26BK01624671_000이 top-5에 뜨지 않았고, "eLoran" 단독
검색도 인덱스 전체에서 매치 0건이었다.**

### 원인 분석

BM25가 못 찾은 게 아니라, **"eLoran"이라는 단어 자체가 인덱스에 존재하지
않았다.** 원본 문서(`R26BK01624671_000_doc01`)를 직접 확인한 결과, 원인이
두 단계로 겹쳐 있었다.

**근본 원인 — 추출 단계의 표 태그 경계 오인식(`parsing/` 소관)**

원문에서 `[표]`/`[/표]` 개수는 정확히 2쌍이지만, 첫 `[표]`(위치 1)와 그에
대응하는 `[/표]`(위치 7093) 사이에 **서로 다른 4개 문서 섹션**(청렴계약서약
안내문 / "1. 입찰개요"(사업명="eLoran 수신기 보정지도 지원 시스템 구축" 포함)
/ "2. 입찰일시 및 장소" / "3. 입찰참가자격" / "3-1. 제출서류")이 **하나의
표 블록으로 뭉쳐 있었다**(총 7,096자). SAMS 앰프 스펙표처럼 같은 종류의
행이 반복되는 진짜 데이터 테이블이 아니라, 원본 PDF/HWP에서 해당 페이지가
테두리 있는 폼 레이아웃으로 렌더링돼 있어 추출기가 페이지 전체(또는 그
상당 부분)를 표 하나로 잘못 인식한 것으로 추정된다. 이건 청킹(`chunker.py`)
소관이 아니라 `parsing/`의 텍스트 추출 로직 소관 — **수정 시 현재 운영 중인
추출 Lambda(pdf/hwp/hwpx)에 미치는 영향 범위를 먼저 확인해야 한다.**

**증상 악화 — 청킹의 표 절단 로직**

경계 오인식으로 생긴 7,096자짜리 블록을, 청킹 로직(기존에 merge된 표 잘림
처리: 768자 초과 시 완전 행 단위 절단, 앞부분만 유지)이 **691자만 남기고
절단**했다. "eLoran"이 있던 위치(1,445)는 절단선 밖이라 **살아남은 청크에
"eLoran"이라는 문자열 자체가 없다.** 즉 근본 원인(경계 오인식)이 만든 거대
블록을, 앞부분만 남기는 절단 전략이 핵심 식별 정보(사업명)까지 함께
날려버려 증상을 악화시켰다.

청크 데이터에 없는 정보는 BM25든 knn이든 원천적으로 못 찾으므로, 이번
케이스는 BM25 vs knn의 성능 차이가 아니다.

### 대응 방향

- **단기 완화책(청킹, 추출 수정 없이 가능)**: 표 절단을 "앞부분만 남기고
  버림"에서 "768자 단위로 순차 분할(여러 청크로 나눠 전량 보존)"로 바꾼다.
  근본 원인(경계 오인식)은 그대로 남지만, 최소한 정보 손실 자체는 막을 수
  있다.
- **근본 해결책(추출, 별도 작업으로)**: `parsing/`의 표 경계 인식 로직을
  개선해 이런 다중 섹션 뭉침을 방지한다. 운영 중인 추출 Lambda(pdf/hwp/hwpx)
  영향 범위 확인이 선행돼야 하는 별도 이슈.

### 미해결 질문

전체 2,624개 청크 중 truncated 319건(약 12%) 안에서, 이번 사례처럼 **표
태그 경계 오인식으로 여러 섹션이 뭉친 "가짜 대형 표"**가 몇 건이고, SAMS
앰프 스펙표처럼 **정말로 행이 많은 데이터 테이블**이 몇 건인지 아직 구분하지
않았다. 이 비율에 따라 단기 완화책(분할)만으로 충분한지, 근본 해결(추출기
수정)의 우선순위를 얼마나 높여야 하는지가 달라진다 — 다음 단계에서 확인 필요.

## 7. 종합

- 임베딩(Cloudflare BGE-M3) → OpenSearch knn 인덱싱 → 검색까지 파이프라인
  자체는 정상 동작 확인(2,624건 전체 성공, 무관 질문에 스코어 유의미하게
  하락)
- 하이브리드 검색(BM25+knn) 도입은 여전히 유효한 개선 방향이지만, 이번
  테스트로는 근거를 직접 확인하지 못했다(정답 문서가 절단으로 아예 빠져
  있었기 때문) — 아래 데이터 손실 문제를 먼저 손본 뒤 재검증이 필요하다.
- 더 시급한 발견은 검색 알고리즘이 아니라 **데이터 손실**이다 — 추출 단계의
  표 경계 오인식(근본 원인, `parsing/` 소관)과 청킹의 절단 전략(증상 악화)이
  겹쳐 문서 식별 정보가 통째로 유실되는 실제 사례를 하나 구체적으로 찾아냈다.

## 8. 청킹 수정 후 재검증

6절에서 발견한 "증상 악화" 요인(표 절단 시 앞부분만 유지)을 단기 완화책대로
수정했다: `embedding/chunker.py`의 표/박스 절단 로직을 제거하고, 한도(768자)
초과 시 행 단위로 여러 청크에 순차 분할(전량 보존, 오버랩 없음)하도록
변경했다. 근본 원인(추출 단계 표 태그 경계 오인식, `parsing/` 소관)은 이번
수정 범위 밖으로 남아있다.

### 8-1. 재청킹 결과

| 항목 | 수정 전 | 수정 후 |
|---|---|---|
| 대상 문서 | 68개 | 69개(Airflow 추가 수집분 1건 포함) |
| 총 청크 수 | 2,624 | **3,357** |
| truncated(절단) | 319건 | **0건**(필드 자체 제거) |
| 표/박스 분할 | 0건 | **319건**(기존 truncated였던 표가 전부 분할로 전환) |
| 불변식 위반(청크가 768자 초과) | 0건 | **0건**(유지) |
| eLoran 사례(7,096자 표) | 691자만 남고 절단, "eLoran" 유실 | **10개 청크로 분할**, "eLoran" 포함 청크 존재 확인 |
| SAMS 스펙표(22,092자) | 733자만 남고 절단 | **31개 청크로 분할** |

### 8-2. 재임베딩 + 재인덱싱

- 3,357개 청크 전체 임베딩: **155.4초, 청크당 46ms**(수정 전과 처리 속도 동일)
- 신규 인덱스 `embedding-experiment-chunks-v2`에 적재(기존 `embedding-experiment-chunks`(v1, 2,624건)는 보존, 나란히 비교 가능)
- v2 적재: 3,357건 전체 성공, 실패 0건

### 8-3. "eLoran" BM25 단독 검색 재검증

| 인덱스 | 결과 |
|---|---|
| v1(수정 전) | **매치 0건** — 단어 자체가 인덱스에 없었음 |
| v2(수정 후) | **1건 매치, score=7.6362, bid_id=R26BK01624671_000, chunk_idx=2** — 정답 문서가 정확히 1위로 검색됨 |

청킹 수정이 의도대로 작동함을 직접 확인했다. `chunk_idx=2`의 내용은
`"○ | 공고명: | eLoran 수신기 보정지도 지원 시스템 구축 ○ | 계약구분: |
총액계약 ..."`로, 사업명을 포함한 입찰개요 섹션이 온전한 청크로 살아있다.

### 8-4. knn 3종 쿼리 재실행(v1 vs v2 비교)

**5-1. "eLoran 수신기 보정지도 지원 시스템 구축 공고의 입찰 참가자격은?"**

| 순위 | v1 score / bid_id | v2 score / bid_id |
|---|---|---|
| 1 | 0.7828 / R26BK01620506_001 | 0.7790 / R26BK01623742_000 |
| 2 | 0.7783 / R26BK01624656_000 | 0.7771 / R26BK01624763_000 |
| 3 | 0.7758 / R26BK01623284_000 | 0.7735 / R26BK01623282_000 |
| 4 | 0.7732 / R26BK01623172_000 | 0.7732 / R26BK01623760_000 |
| 5 | 0.7732 / R26BK01623760_000 | 0.7731 / R26BK01623172_000 |

**정답 문서(R26BK01624671_000)는 v2에서도 knn top-5에 없다** — "eLoran"
텍스트가 이제 코퍼스에 존재함에도 knn이 여전히 못 찾는다. 이건 **하이브리드
검색이 필요하다는 걸 이번에 처음으로 직접 확인한 것**이다(6절 시점엔 정답
문서 자체가 절단으로 빠져 있어 근거를 못 만들었었다) — BM25는 정답을 1위로
정확히 찾았는데(8-3), knn은 의미적으로 "입찰참가자격" 근방인 다른 문서들만
비슷한 스코어로 나열한다. 정확한 고유명사/식별자 검색은 BM25가, 의미
기반 탐색은 knn이 맡는 하이브리드 조합이 실제로 상호보완적임을 확인.

**5-2. "소프트웨어 개발 용역 공고"** — v1과 v2 모두 "기술용역"/"소프트웨어"
계열 문서가 상위권(score 0.77~0.79대), 순위 구성원은 청크 분할 방식이 달라져
일부 바뀌었으나(예: R26BK01623284_000 표 청크가 1위로 새로 등장) 전반적
경향은 동일하게 유지됨.

**5-3. "김치찌개 만드는 법"** — v1(0.6821~0.6716) 대비 v2(0.6939~0.6744)로
절대 스코어가 근소하게 상승했으나(청크 분할로 표 청크의 개별 텍스트가 더
짧고 특정 소재(수지 시공/절단 가공 등)에 집중되며 우연히 소폭 상승한 것으로
추정), 여전히 5-1/5-2(0.77~0.79대) 대비 **약 0.08~0.10 낮은 구간**을
유지 — 무관한 질문을 낮은 유사도로 구분하는 패턴은 청킹 변경 후에도 그대로
성립한다.

## 9. 종합(갱신)

- 청킹 수정(표 절단 → 분할)으로 데이터 손실 문제는 해결 확인(truncated
  0건, eLoran 사례 포함 재현 확인)
- 재수정 후에도 knn 단독으로는 이번 실패 케이스(정확한 사업명 매칭)를 못
  찾고, BM25는 정확히 찾는다는 걸 직접 확인 — **하이브리드 검색(BM25+knn)
  도입을 다음 단계로 명확히 권고**한다(더 이상 가설이 아니라 실측 근거 있음)
- 남은 것: 추출 단계의 표 경계 오인식(근본 원인, `parsing/` 소관, 운영 중인
  추출 Lambda 영향 범위 확인 필요)은 이번 범위 밖 — 별도 이슈로 진행 필요.
  truncated 319건 중 "섹션 뭉침"형 vs "진짜 데이터 테이블"형 비율도 여전히
  미확인(이제는 truncated 필드가 없어져 `block_split` 로그(319건)로 같은
  구분 조사가 가능)

## 10. 하이브리드 검색(BM25+knn) 구축 — 로컬 검증 마지막 단계

9절에서 실측 근거로 확인한 하이브리드 검색 필요성을 실제로 구축하고
검증한다.

### 10-1. 구성

| 요소 | 방식 |
|---|---|
| 한국어 형태소 분석 | OpenSearch `analysis-nori` 플러그인 설치(`docker exec` + 컨테이너 재시작, `db/docker-compose.yml` 상단 주석에 재설치 절차 문서화 — plugins 디렉토리가 named volume이 아니라 컨테이너 재생성 시 유실됨) |
| 인덱스 | `embedding-experiment-chunks-v3` — v2와 매핑 동일, `text` 필드만 커스텀 nori 분석기(`nori_tokenizer` + `nori_part_of_speech` stopword 필터 + lowercase) 적용. `embedded_chunks.json`(3,357개, v2와 동일 벡터) 재적재 — **재임베딩 없음** |
| 결합 방식 | OpenSearch search pipeline(`hybrid-search-pipeline`) — `normalization-processor`로 BM25/knn 스코어를 min-max 정규화 후 산술평균으로 가중 결합(`experiments/embedding/hybrid_search.py`). 가중치는 CLI 인자(`--bm25-weight`/`--knn-weight`, 기본 0.5/0.5)로 조정 가능 |

nori 토크나이저 동작 확인(`"eLoran 수신기 보정지도 지원 시스템 구축 공고의
입찰 참가자격은 무엇인가?"`):
```
["eloran", "수신", "기", "보정", "지도", "지원", "시스템", "구축", "공고", "입찰", "참가", "자격", "무엇", "이"]
```
조사 제거, 영문 소문자 정규화, 명사 위주 분리 확인(`수신기`→`수신`+`기`는
사용자 사전 없는 nori 기본 동작, 버그 아님).

### 10-2. 3방식 비교 (`experiments/embedding/run_hybrid_comparison.py`, 가중치 0.5/0.5)

**쿼리 1 — "eLoran 수신기 보정지도 지원 시스템 구축 공고의 입찰 참가자격은 무엇인가?"**

| 순위 | BM25만 | knn만 | 하이브리드 |
|---|---|---|---|
| 1 | 34.31 / **R26BK01624671_000**(정답) | 0.7790 / R26BK01623742_000 | 0.5005 / R26BK01623742_000 |
| 2 | 18.68 / R26BK01620506_001 | 0.7771 / R26BK01624763_000 | **0.5000 / R26BK01624671_000(정답)** |
| 3 | 17.29 / R26BK01620506_001 | 0.7758 / R26BK01623284_000 | 0.3382 / R26BK01624763_000 |
| 4 | 16.50 / R26BK01622921_000 | 0.7732 / R26BK01623760_000 | 0.2275 / R26BK01623284_000 |
| 5 | 16.32 / R26BK01623742_000 | 0.7731 / R26BK01623172_000 | 0.0656 / R26BK01620506_001 |

**BM25만으로도(nori 적용 후) 정답 문서가 1위**, knn만으로는 여전히 top-5
밖(9절과 동일 — 같은 벡터라 당연함). **하이브리드는 정답 문서를 2위로
살려낸다** — BM25의 정확 매치가 정규화·결합 과정에서 죽지 않고 살아남음을
확인. "BM25가 살려주는지" 검증 완료.

**쿼리 2 — "소프트웨어 개발 용역 공고"**

| 순위 | BM25만 | knn만 | 하이브리드 |
|---|---|---|---|
| 1 | 10.76 / R26BK01623573_000(DBMS/보안 요구사항 표) | 0.7893 / R26BK01622057_000 | 0.5000 / R26BK01623573_000 |
| 2 | 9.89 / R26BK01623261_000 | 0.7893 / R26BK01622272_000 | 0.5000 / R26BK01622272_000 |
| 3 | 9.84 / R26BK01624318_000 | 0.7834 / R26BK01621591_000 | 0.5000 / R26BK01622057_000 |
| 4 | 9.43 / R26BK01622057_000 | 0.7828 / R26BK01624318_000 | 0.1742 / R26BK01623261_000 |
| 5 | 9.43 / R26BK01622272_000 | 0.7816 / R26BK01621591_000 | 0.1532 / R26BK01624318_000 |

BM25와 knn이 서로 다른 문서를 최상위로 뽑는다(BM25는 "TSDBMS/시스템 관리"
같은 구체적 기술 용어 매치, knn은 "사업금액 20억원 미만" 같은 반복 등장하는
정형 문구 위주). **하이브리드는 두 방식의 상위권을 섞어서** top-5 안에
BM25 1위와 knn 1·2위가 모두 들어간다 — "knn 강점 유지" 확인(knn이 상위권에
꼽은 문서들이 하이브리드에서도 살아남음).

**쿼리 3 — "김치찌개 만드는 법"**

| 순위 | BM25만 | knn만 | 하이브리드 |
|---|---|---|---|
| 1 | 6.86 / R26BK01623782_000(물막이공 표) | 0.6939 / R26BK01623782_000 | 0.5000 / R26BK01623782_000 |
| 2 | 6.86 / R26BK01623782_000 | 0.6934 / R26BK01623782_000 | 0.5000 / R26BK01623782_000 |
| 3 | 5.46 / R26BK01623782_000 | 0.6821 / R26BK01624022_000 | 0.5000 / R26BK01623782_000 |
| 4 | 5.46 / R26BK01623782_000 | 0.6771 / R26BK01623782_000 | 0.4874 / R26BK01623782_000 |
| 5 | 5.20 / R26BK01623782_000 | 0.6744 / R26BK01623782_000 | 0.1989 / R26BK01624022_000 |

절대 순위 내용은 여전히 무관하지만(예상대로 — 코퍼스에 김치찌개 관련 내용이
없으므로 무언가는 뜬다), **판별 신호는 두 방식 모두에서 관련 쿼리 대비
낮게 나온다**: BM25 최고점이 쿼리1(34.31)·쿼리2(10.76) 대비 쿼리3(6.86)에서
확연히 낮고, knn도 쿼리1·2(0.77~0.79대) 대비 쿼리3(0.67~0.69대)에서 낮다.
"이중 신호로 더 선명해지는지"는 절반만 확인됨 — **BM25 단독 신호는
스코어 자체가 작아서(6.86 vs 34.31) 상대적 판별력이 오히려 knn보다 뚜렷하고,
두 신호가 같은 방향(낮음)으로 일치한다는 점에서 교차검증 효과는 있지만**,
BM25 raw score가 0에 가깝게 떨어지진 않는다(코퍼스에 흔한 단어 "법" 등의
부분 매치로 완전 0은 아님) — 하이브리드 스코어 자체만으로 "무관함"을 자동
판별하는 절대 임계값을 만들긴 아직 이르고, 상대적 스코어 격차 기반 판단이
여전히 필요하다.

### 10-3. 종합

3가지 목표 전부 확인됨:
1. **BM25가 살려주는가** — 그렇다. 정답 문서가 knn 단독으론 안 뜨지만
   하이브리드에선 2위로 복귀.
2. **knn 강점이 유지되는가** — 그렇다. 탐색형 쿼리에서 knn이 찾은 상위
   문서들이 하이브리드 결과에도 반영됨.
3. **무관 판별이 더 선명해지는가** — 부분적으로. 두 신호가 같은 방향(낮은
   점수)으로 일치해 교차검증은 되지만, 자동 임계값 판별을 위한 추가 튜닝
   여지는 남아있음(가중치 조정, 최소 스코어 컷오프 등 — 다음 단계 후보).

10-3까지는 쿼리 3개로 하이브리드가 정성적으로 동작하는지 확인한 것 —
11절에서 평가셋 111개로 정량적으로(recall/MRR) 재확인한다.

## 11. 하이브리드 가중치 스윕 + recall 측정

### 11-1. 평가셋

`experiments/embedding/eval_set.json` — 42개 고유 공고 중 39개(3개는
첨부문서가 완전히 동일한 일반 규정문서뿐이라 식별 정보가 없어 제외)에서
공고당 2~3개, 총 111개 쿼리를 **LLM API 없이 직접(수동으로) 생성**했다.
원문 문장을 그대로 베끼지 않고 동의어·구어체·업종 용어로 바꿔 쓰는 제약을
지켰고, 유형 3종(탐색형/조건매칭형/공고내부형)을 공고당 최소 1개씩 섞었다.
같은 사업이 취소·재공고로 두 bid_id에 걸쳐 존재하는 경우(아라3중학교 신축
정보통신공사, `R26BK01608505_001`↔`R26BK01624544_000`)는 `answer_bid_id`를
리스트로 허용하고 `same_project_group` 필드로 태그했다.

### 11-2. 튜닝/검증 분할

`experiments/embedding/split_eval_set.py` — 유형별로 각각 70:30 분할 후
합쳐서(시드 42 고정, 재실행해도 동일) 유형 비율을 두 셋에서 거의 동일하게
유지했다.

| | 전체 | 탐색형 | 조건매칭형 | 공고내부형 |
|---|---|---|---|---|
| 튜닝셋 | 78 | 27 | 24 | 27 |
| 검증셋 | 33 | 11 | 11 | 11 |

### 11-3. 측정 방법

`experiments/embedding/run_recall_eval.py`:

- **평가 단위는 "공고"(bid_id)** — 문서 하나가 청크 수십 개로 쪼개져 있어
  원시 검색 결과 상위권이 같은 공고의 청크로 도배될 수 있다. 그래서 원시
  hit 50개(`CANDIDATE_SIZE`)를 처음 등장한 순서로 bid_id 기준 중복 제거한
  뒤 그 순위로 recall@5/recall@10/MRR을 계산했다. `answer_bid_id`가
  리스트면 그중 하나라도 top-k 안에 들면 hit.
- **비교 대상**: BM25:knn 가중치 0:100(=knn 단독)/30:70/50:50/70:30/
  100:0(=BM25 단독) — OpenSearch search pipeline(normalization-processor,
  min-max 정규화+가중 산술평균)으로 실행. **RRF**(Reciprocal Rank Fusion,
  `1/(k+rank)`, k=60)는 파이프라인이 아니라 스크립트에서 직접 계산 —
  설치된 OpenSearch 2.19.1의 neural-search 플러그인이 rrf 결합 기법을
  지원하는지 불확실해서, BM25/knn 각각의(공고 단위로 중복 제거한) 순위
  리스트에 표준 공식을 직접 적용하는 쪽을 택했다.
- **방법론 참고사항**: 이번 평가는 knn 후보를 k=50으로 넉넉히 가져오는데,
  10절에서 확인했던 "knn 단독으로는 eLoran 문서가 top-5 밖"이라는 결과는
  k=5 기준이었다. k=50으로 늘리면 HNSW 근사 탐색이 그래프를 더 넓게
  탐색해서 같은 쿼리에서도 진짜 최근접 후보를 더 잘 찾아내는 경우가
  있었다(실제로 이번 평가에서는 eLoran 문서가 knn 단독 1위로 나옴) — k가
  커지면 근사 탐색 정확도가 올라가는 정상적인 HNSW 특성이지, 이전 결과가
  틀렸던 게 아니다.

### 11-4. 튜닝셋 결과 (78개)

| 설정 | 전체 R@5 | 전체 R@10 | 전체 MRR | 탐색+조건 R@5 | 탐색+조건 R@10 | 탐색+조건 MRR | 공고내부 R@5 | 공고내부 R@10 | 공고내부 MRR |
|---|---|---|---|---|---|---|---|---|---|
| 0:100(knn만) | 0.910 | 0.923 | 0.832 | 0.902 | 0.922 | 0.813 | 0.926 | 0.926 | 0.867 |
| 30:70 | 0.923 | 0.974 | 0.893 | 0.922 | 0.980 | 0.881 | 0.926 | 0.963 | 0.914 |
| **50:50** | **0.962** | **1.000** | **0.909** | 0.941 | 1.000 | 0.881 | **1.000** | **1.000** | **0.963** |
| 70:30 | 0.949 | 1.000 | 0.904 | 0.922 | 1.000 | 0.868 | 1.000 | 1.000 | 0.972 |
| 100:0(BM25만) | 0.949 | 0.962 | 0.891 | 0.922 | 0.941 | 0.849 | 1.000 | 1.000 | 0.970 |
| RRF | 0.923 | 0.974 | 0.886 | 0.902 | 0.980 | 0.860 | 0.963 | 0.963 | 0.936 |

recall@10=0 쿼리(50:50 기준): **0건**. 50:50이 전체 R@5·R@10·MRR 전부 최고,
70:30도 근소 차이로 근접.

### 11-5. 검증셋 결과 (33개) — 0:100/50:50/70:30/100:0

| 설정 | 전체 R@5 | 전체 R@10 | 전체 MRR | 탐색+조건 R@5 | 탐색+조건 R@10 | 탐색+조건 MRR | 공고내부 R@5 | 공고내부 R@10 | 공고내부 MRR |
|---|---|---|---|---|---|---|---|---|---|
| 0:100(knn만) | 0.848(28/33) | 0.879(29/33) | 0.760 | 0.773 | 0.818 | 0.663 | 1.000 | 1.000 | 0.955 |
| **50:50** | **1.000(33/33)** | **1.000(33/33)** | 0.898 | **1.000** | **1.000** | 0.848 | 1.000 | 1.000 | 1.000 |
| 70:30 | 0.970(32/33) | 1.000(33/33) | 0.917 | 0.955 | 1.000 | 0.875 | 1.000 | 1.000 | 1.000 |
| 100:0(BM25만) | 0.970(32/33) | 1.000(33/33) | 0.923 | 0.955 | 1.000 | 0.884 | 1.000 | 1.000 | 1.000 |

**50:50 vs 70:30 차이는 정확히 1개 쿼리**(33개 중):
```
쿼리: "대기업은 못 들어가고 중소·중견만 참여 가능한 소프트웨어 납품 건인가"
정답: R26BK01622057_000
50:50 → 5위(recall@5 hit)   70:30 → 6위(recall@5 miss)
```

**recall@10=0 쿼리는 knn 단독(0:100)에서만 4건**, 50:50/70:30/100:0은
0건.

| 유형 | 쿼리 | 정답 | knn 단독이 대신 상위에 올린 것 |
|---|---|---|---|
| 탐색형 | "군 장병용 원격진료 시스템 구매 공고 있나" | R26BK01623284_000 | 620506, 622057, 622272, 624671, 623742 (전부 무관) |
| 탐색형 | "화학물질 확산평가 모델 개발 관련 용역 공고 찾는 중" | R26BK01622921_000 | 622272, 622057, 624191, 624063, 623573 (전부 무관) |
| 조건매칭형 | "특고압 케이블 제조업체면 참여 가능한 공고 있어?" | R26BK01610832_001 | 551048, 623742, 624318, 622081, 624449 (전부 무관) |
| 조건매칭형 | "학술연구용역 업종 등록해야 참여 가능한 국토부 연구 용역인가" | R26BK01621346_000 | 623039, 620445, 620050, 622081, 620154 (전부 무관) |

이 4건은 라벨 오류가 아니라(정답 자체는 코퍼스에 존재·확인됨) knn
단독이 의미적으로 비슷한 다른 공고와 혼동한 사례 — **하이브리드로
바뀌면 이 4건 모두 recall@10=0에서 벗어난다.**

### 11-6. 결론

- **하이브리드(BM25+knn) 채택.** 튜닝·검증 양쪽에서 knn 단독 대비
  일관되게 우위(검증셋 기준 knn 단독 recall@10=0 4건 → 하이브리드 0건,
  R@5도 0.848→1.000). 다만 BM25 단독과 비교하면 우위가 그렇게 크지
  않다(검증셋 R@5 100:0=0.970 vs 50:50=1.000, 33개 중 1건 차이) — 이번
  평가셋에서 하이브리드의 확실한 이득은 "knn 단독 대비"이고, "BM25 단독
  대비"는 미세한 개선에 가깝다.
- **기본 가중치는 50:50 채택.** 근거는 recall 소수점 차이가 아니라
  **강건성**이다 — 70:30은 검증셋에서 조건매칭형 쿼리 1건이 5위에서
  6위로 밀려 R@5를 놓쳤다(BM25 비중을 올릴수록 knn이 구제하던 케이스가
  하나씩 깎여나가는 패턴). 50:50은 튜닝·검증 양쪽에서 가장 안정적으로
  최상위권을 유지했다.
- **knn 단독 실패 4개 쿼리를 "하이브리드 필요성 사례"로 명시**한다(11-5
  표) — 이 4건이 이번 평가셋에서 하이브리드 도입을 정당화하는 가장
  구체적인 증거다.

### 11-7. 한계 (반드시 함께 기록)

- **42개 공고 소형 코퍼스 + 문서에서 역생성한 쿼리라 절대 수치는 낙관
  편향이 있다.** 쿼리를 만든 사람(나)이 정답 문서 원문을 이미 읽고
  썼기 때문에, 실제 사용자가 사전 지식 없이 던지는 쿼리보다 검색이
  쉬울 수밖에 없다. 검증셋 50:50 만점(R@5·R@10 33/33)은 "완벽한 검색
  시스템"이 아니라 **"이 평가셋 설계상 나올 수 있는 천장에 도달했다"**로
  해석해야 한다.
- **이 결과는 방향성(하이브리드가 knn 단독보다 낫다) 확인용이며, 정밀한
  최적 가중치 산정이나 절대 성능 보증이 아니다.** 50:50과 70:30의
  차이가 33개 중 1개 쿼리 수준이라는 것 자체가, 이 표본 크기로는
  가중치를 소수점 단위로 정밀 튜닝할 근거가 못 됨을 보여준다. 코퍼스가
  커지고(현재 42개 공고 → 실제 운영 규모) 실사용 검색 로그가 쌓인
  뒤 재측정이 필요하다.
- **평가셋 생성/분할/스윕 스크립트는 전부 재사용 가능한 자산으로
  보존한다** — `generate_eval_set.py`(LLM 자동생성 버전, 이번엔 안
  씀), `split_eval_set.py`, `run_recall_eval.py`(`--weights` 인자로
  가중치 조합 재실행 가능). 코퍼스가 커지면 같은 스크립트로 재측정하면
  된다.

로컬 검증은 여기서 마무리. 다음은 이 구성(nori 인덱스 + 하이브리드
파이프라인, 기본 가중치 50:50)을 실제 파이프라인(Lambda 편입, AWS
관리형 OpenSearch로의 이전)으로 옮기는 단계.
